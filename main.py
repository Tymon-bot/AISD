from typing import Any, List

class TreeNode:
    value: Any
    children: List['TreeNode']
    parent: Any

    def __innit__(self, data):
        self.data = data
        self.children = List


    def is_leaf()->bool:
        if TreeNode is null:
            return 0
        if TreeNode is not null:
            return 1


    def add(child: 'TreeNode')->None:
        new_node = TreeNode(value, None)



class Tree:
    root: TreeNode

    def __init__(self,data):
        self.data = data
        self.parent = parent

    def add(value: Any, parent_name:Any)-> None:
        new_node = TreeNode(value, parent_name)
        if parent_name is  None:
            root = new_node
        else:
            return



leaf = TreeNode()
leaf.data = 'TreeNode'
print(leaf.data)



